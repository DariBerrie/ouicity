<div class="container text-center ">


   <div class="map-container d-flex justify-content-center mt-5">
    <%= image_tag "https://api.mapbox.com/styles/v1/mapbox/streets-v11/static/2.3381,48.8693,10.45,0,0/1000x400?access_token=#{ENV['MAPBOX_API_KEY']}",
        class: "rounded-4",
        style: "width: 1200px; height: 400px; object-fit:cover;" %>
  </div>

  <h2 class="my-4">Send an alert</h2>

<%# Search button %>
  <div class="row justify-content-center">
    <div class="col-lg-6 col-sm-6">
      <%= form_with url: alerts_path, method: :get, class: "d-flex" do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control fs-4",
          placeholder: "Enter the address"
        %>
        <%= submit_tag "->", class: "btn btn-primary rounded-right fs-4 text-white" %>
      <% end %>
  </div>
  </div>

    <div class="d-flex justify-content-center mt-4 ">
      <h3>Alerts in this area</h3>
      <%= link_to "↓", "#", class: "btn btn-primary rounded-right fs-4 text-white toggle" %>
    </div>

    <%# Alerts %>
    <div class="shadow alerts-container">
      <% @alerts.each do |alert| %>
      <%= render 'shared/alert_banner', alert: alert %>
      <% end %>
      <h3 class="mt-3">Don't see your issue above?</h3>
      <%= link_to "Send a new alert", new_alert_path, class: "btn btn-primary rounded-4 mb-4" %>

    </div>



</div>
